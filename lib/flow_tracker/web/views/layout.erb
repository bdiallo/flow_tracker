<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>FlowTracker</title>
  <style>
    :root {
      --bg-primary: #0f172a;
      --bg-secondary: #1e293b;
      --bg-tertiary: #334155;
      --text-primary: #f1f5f9;
      --text-secondary: #94a3b8;
      --text-muted: #64748b;
      --accent: #3b82f6;
      --accent-hover: #2563eb;
      --success: #22c55e;
      --warning: #f59e0b;
      --error: #ef4444;
      --border: #475569;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background-color: var(--bg-primary);
      color: var(--text-primary);
      line-height: 1.5;
      min-height: 100vh;
    }

    .container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 20px;
    }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 30px;
      padding-bottom: 20px;
      border-bottom: 1px solid var(--border);
    }

    .header h1 {
      font-size: 24px;
      font-weight: 600;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .header h1 a {
      color: var(--text-primary);
      text-decoration: none;
    }

    .header-actions {
      display: flex;
      gap: 10px;
    }

    /* Navigation */
    nav {
      display: flex;
      gap: 20px;
      margin-bottom: 30px;
    }

    nav a {
      color: var(--text-secondary);
      text-decoration: none;
      padding: 8px 16px;
      border-radius: 6px;
      transition: all 0.2s;
    }

    nav a:hover, nav a.active {
      color: var(--text-primary);
      background-color: var(--bg-secondary);
    }

    /* Stats Grid */
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 16px;
      margin-bottom: 30px;
    }

    .stat-card {
      background-color: var(--bg-secondary);
      border-radius: 8px;
      padding: 20px;
      text-align: center;
    }

    .stat-card .value {
      font-size: 32px;
      font-weight: 700;
      color: var(--text-primary);
    }

    .stat-card .label {
      font-size: 14px;
      color: var(--text-secondary);
      margin-top: 4px;
    }

    .stat-card.running .value { color: var(--accent); }
    .stat-card.completed .value { color: var(--success); }
    .stat-card.failed .value { color: var(--error); }

    /* Table */
    .table-container {
      background-color: var(--bg-secondary);
      border-radius: 8px;
      overflow: hidden;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    th, td {
      padding: 12px 16px;
      text-align: left;
      border-bottom: 1px solid var(--border);
    }

    th {
      background-color: var(--bg-tertiary);
      font-weight: 600;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--text-secondary);
    }

    tr:hover {
      background-color: var(--bg-tertiary);
    }

    tr:last-child td {
      border-bottom: none;
    }

    /* Links */
    a {
      color: var(--accent);
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    /* Status Badge */
    .status-badge {
      display: inline-block;
      padding: 4px 10px;
      border-radius: 4px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
    }

    .status-running { background-color: rgba(59, 130, 246, 0.2); color: var(--accent); }
    .status-completed { background-color: rgba(34, 197, 94, 0.2); color: var(--success); }
    .status-failed { background-color: rgba(239, 68, 68, 0.2); color: var(--error); }
    .status-skipped { background-color: rgba(148, 163, 184, 0.2); color: var(--text-secondary); }

    /* Log Level Badge */
    .level-badge {
      display: inline-block;
      padding: 2px 8px;
      border-radius: 3px;
      font-size: 11px;
      font-weight: 600;
      text-transform: uppercase;
    }

    .level-debug { background-color: rgba(148, 163, 184, 0.2); color: var(--text-secondary); }
    .level-info { background-color: rgba(59, 130, 246, 0.2); color: var(--accent); }
    .level-warn { background-color: rgba(245, 158, 11, 0.2); color: var(--warning); }
    .level-error { background-color: rgba(239, 68, 68, 0.2); color: var(--error); }

    /* Button */
    .btn {
      display: inline-block;
      padding: 8px 16px;
      border-radius: 6px;
      font-size: 14px;
      font-weight: 500;
      text-decoration: none;
      cursor: pointer;
      border: none;
      transition: all 0.2s;
    }

    .btn-primary {
      background-color: var(--accent);
      color: white;
    }

    .btn-primary:hover {
      background-color: var(--accent-hover);
      text-decoration: none;
    }

    .btn-danger {
      background-color: var(--error);
      color: white;
    }

    .btn-danger:hover {
      background-color: #dc2626;
      text-decoration: none;
    }

    .btn-secondary {
      background-color: var(--bg-tertiary);
      color: var(--text-primary);
    }

    .btn-secondary:hover {
      background-color: var(--border);
      text-decoration: none;
    }

    /* Card */
    .card {
      background-color: var(--bg-secondary);
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 20px;
    }

    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 16px;
      padding-bottom: 16px;
      border-bottom: 1px solid var(--border);
    }

    .card-title {
      font-size: 18px;
      font-weight: 600;
    }

    /* Filter Form */
    .filter-form {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      margin-bottom: 20px;
      padding: 16px;
      background-color: var(--bg-secondary);
      border-radius: 8px;
    }

    .filter-form input,
    .filter-form select {
      padding: 8px 12px;
      border-radius: 6px;
      border: 1px solid var(--border);
      background-color: var(--bg-tertiary);
      color: var(--text-primary);
      font-size: 14px;
    }

    .filter-form input:focus,
    .filter-form select:focus {
      outline: none;
      border-color: var(--accent);
    }

    /* Flash Message */
    .flash {
      padding: 12px 16px;
      border-radius: 6px;
      margin-bottom: 20px;
      background-color: rgba(34, 197, 94, 0.2);
      color: var(--success);
    }

    /* Process Detail */
    .process-meta {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 16px;
      margin-bottom: 20px;
    }

    .meta-item {
      padding: 12px;
      background-color: var(--bg-tertiary);
      border-radius: 6px;
    }

    .meta-item .label {
      font-size: 12px;
      color: var(--text-secondary);
      margin-bottom: 4px;
    }

    .meta-item .value {
      font-size: 14px;
      color: var(--text-primary);
      word-break: break-all;
    }

    /* Flow Tree */
    .flow-tree {
      font-family: 'SF Mono', Monaco, 'Courier New', monospace;
      font-size: 13px;
    }

    .flow-item {
      padding: 8px 12px;
      margin: 4px 0;
      background-color: var(--bg-tertiary);
      border-radius: 4px;
      border-left: 3px solid var(--border);
    }

    .flow-item.status-completed { border-left-color: var(--success); }
    .flow-item.status-failed { border-left-color: var(--error); }
    .flow-item.status-running { border-left-color: var(--accent); }

    .flow-item .flow-name {
      font-weight: 600;
      color: var(--text-primary);
    }

    .flow-item .flow-meta {
      font-size: 12px;
      color: var(--text-secondary);
      margin-top: 4px;
    }

    /* Log Entries */
    .log-entries {
      font-family: 'SF Mono', Monaco, 'Courier New', monospace;
      font-size: 12px;
      background-color: var(--bg-tertiary);
      border-radius: 6px;
      padding: 12px;
      max-height: 400px;
      overflow-y: auto;
    }

    .log-entry {
      padding: 4px 0;
      border-bottom: 1px solid var(--border);
    }

    .log-entry:last-child {
      border-bottom: none;
    }

    .log-entry .timestamp {
      color: var(--text-muted);
      margin-right: 8px;
    }

    .log-entry .message {
      color: var(--text-primary);
    }

    /* Error Display */
    .error-box {
      background-color: rgba(239, 68, 68, 0.1);
      border: 1px solid var(--error);
      border-radius: 6px;
      padding: 16px;
      margin-top: 16px;
    }

    .error-box pre {
      font-family: 'SF Mono', Monaco, 'Courier New', monospace;
      font-size: 12px;
      white-space: pre-wrap;
      word-break: break-all;
      color: var(--error);
      margin-top: 8px;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .stats-grid {
        grid-template-columns: repeat(2, 1fr);
      }

      .filter-form {
        flex-direction: column;
      }

      .header {
        flex-direction: column;
        gap: 16px;
        align-items: flex-start;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header class="header">
      <h1><a href="<%= root_path %>/">FlowTracker</a></h1>
      <div class="header-actions">
        <form action="<%= root_path %>/cleanup" method="post" style="display: inline;">
          <input type="hidden" name="_method" value="post">
          <button type="submit" class="btn btn-secondary" onclick="return confirm('Delete processes older than 7 days?')">
            Cleanup
          </button>
        </form>
      </div>
    </header>

    <nav>
      <a href="<%= root_path %>/">Dashboard</a>
      <a href="<%= root_path %>/processes">Processes</a>
    </nav>

    <% if params[:flash] %>
      <div class="flash"><%= params[:flash] %></div>
    <% end %>

    <%= yield %>
  </div>
</body>
</html>
